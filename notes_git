git clone
git add
git commit -m
git push origin

to make tokin:
settings -> developer settinngs -> personal access tokens
to add an ssh key:
settings -> ssh and gpg keys -> new ssh key

git remote -v
git remote set-url origin git@github.com:leonbog1984/notes.git

git branch
git checkout branch_name
git checkout -b branch_name1
git checkout master
git merge branch_name1
git branch -d branch_name1
git branch -D branch_name1 (force delete even if commit(s) was/were added)

git chackout fc67b0f47f92d538f7c4d0c61ad50f698e2bef05 (switch to a certain commit)
git reset --hard HEAD~1 (deletes one last commit, so moves HEAD to one commit backward)
git reset --soft HEAD~4 (deletes 4 previous commites, do not move HEAD anywhere)

